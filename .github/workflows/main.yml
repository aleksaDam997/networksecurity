name: workflow

on:
    push:
        branches:
            - main
        path-ignore:
            - 'README.md'
    
    jobs:
        integration:
            name: Continioues Integration
            runs-on: ubuntu-latest
            steps:
                - name: Chckout Code
                uses: actions/checkout@v3

                - name: Lint code
                run: echo "Linting repository"

                - name: Run unit tests
                run: echo "Running unit tests"